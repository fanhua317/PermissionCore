<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.permacore.iam.mapper.SysOperLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.permacore.iam.domain.entity.SysOperLogEntity">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="business_type" property="businessType" />
        <result column="method" property="method" />
        <result column="request_method" property="requestMethod" />
        <result column="operator_id" property="operatorId" />
        <result column="operator_name" property="operatorName" />
        <result column="oper_ip" property="operIp" />
        <result column="oper_location" property="operLocation" />
        <result column="oper_param" property="operParam" />
        <result column="json_result" property="jsonResult" />
        <result column="status" property="status" />
        <result column="error_msg" property="errorMsg" />
        <result column="oper_time" property="operTime" />
        <result column="cost_time" property="costTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, business_type, method, request_method, operator_id, operator_name, oper_ip, oper_location, oper_param, json_result, status, error_msg, oper_time, cost_time
    </sql>

    <!-- 列表不读取大字段；完整内容由按 ID 查询的详情接口返回。 -->
    <sql id="List_Column_List">
        id, title, business_type, method, request_method, operator_id, operator_name,
        oper_ip, oper_location, status, oper_time, cost_time
    </sql>

    <!-- 固定条数查询不走分页插件，避免 recent 接口执行无意义的 COUNT。 -->
    <select id="selectRecent" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List" />
        FROM sys_oper_log
        ORDER BY oper_time DESC, id DESC
        LIMIT #{limit}
    </select>

</mapper>
