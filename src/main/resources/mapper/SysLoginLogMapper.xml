<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.permacore.iam.mapper.SysLoginLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.permacore.iam.domain.entity.SysLoginLogEntity">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="ip_address" property="ipAddress" />
        <result column="location" property="location" />
        <result column="browser" property="browser" />
        <result column="os" property="os" />
        <result column="login_time" property="loginTime" />
        <result column="status" property="status" />
        <result column="message" property="message" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, ip_address, location, browser, os, login_time, status, message
    </sql>

    <!-- 固定条数查询不走分页插件，避免 recent 接口执行无意义的 COUNT。 -->
    <select id="selectRecent" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM sys_login_log
        ORDER BY login_time DESC, id DESC
        LIMIT #{limit}
    </select>

</mapper>
