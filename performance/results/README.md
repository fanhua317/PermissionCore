# Generated performance results

Each timestamped directory is one isolated run. Commit only reviewed compact
artifacts (`environment.json`, `summary.csv`, `ladder.csv`, and selected JSON
summaries) that support a published performance report. The `raw/` subtree is
ignored because high-frequency monitoring data and diagnostics can be large.
